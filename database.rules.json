{
  "rules": {
    ".read": "auth.token.email_verified == true && auth.token.email.matches(/.*@adevinta.com$/)",
    ".write": "auth.token.email_verified == true && auth.token.email.matches(/.*@adevinta.com$/)"
  }
}