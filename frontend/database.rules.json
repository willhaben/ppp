{
  "rules": {
    ".read": "auth.token.email_verified == true && auth.token.email.matches(/.*@schibsted.com$/)",
    ".write": "auth.token.email_verified == true && auth.token.email.matches(/.*@schibsted.com$/)"
  }
}